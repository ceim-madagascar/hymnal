[tool.poetry]
name = "hymnal"
version = "0.1.0"
description = ""
authors = ["gersona <gersona@hotmail.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
